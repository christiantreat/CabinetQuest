# Configuration for redcap_proxy.py -- copy to .env (gitignored) or set in the
# host's secrets manager. The token NEVER goes in the repo or client code.
REDCAP_API_URL=https://redcap.stonybrookmedicine.edu/api/
REDCAP_API_TOKEN=
# Origin(s) of the training site allowed to POST /collect (comma-separated).
ALLOWED_ORIGIN=https://christiantreat.github.io
